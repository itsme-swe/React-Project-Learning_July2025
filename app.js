import React from "react";
import ReactDOM from "react-dom/client";

const heading = <h1 id="heading">Namaste I am JSX 🚀</h1>;

const root = ReactDOM.createRoot(document.getElementById("root")).render(
  heading
);
